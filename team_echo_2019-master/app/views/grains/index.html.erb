
<p id="notice"><%= notice %></p>

<h2 style = "font-family: Helvetica;">Grains</h2>

<!-- The following code was based on a video found on www.driftingruby.com/episodes/importing-and-exporting-csv-data -->

<%= link_to "Export CSV", grains_url(format: :csv), class: 'btn btn-primary' %>
<%= link_to 'New Grain', new_grain_path, class: "btn btn-success" %>

<table>
  <thead>
    <tr>
      <th style = "font-family: Helvetica;">Grain</th>
      <th style = "font-family: Helvetica;">Grain type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @grains.each do |grain| %>
      <tr>
        <td><%= grain.grain_id %></td>
        <td><%= grain.grain_type %></td>
        <td><%= link_to 'Show', grain, class: 'btn btn-secondary' %></td>
        <td><%= link_to 'Edit', edit_grain_path(grain), class: 'btn btn-warning' %></td>
        <td><%= link_to 'Destroy', grain, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Grain', new_grain_path, class: "btn btn-success" %>


